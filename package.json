{
  "name": "first-game",
  "version": "1.0.0",
  "description": "Tennis game built from tutorial.",
  "main": "index.js",
  "scripts": {
    "start": "node ./src/tennis.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d lib"
  },
  "author": "William Kontos",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}
